services:
  app-escolar:
    build: .
    ports:
      - "8080:8080"
    container_name: servlet-container
    volumes:
      - ./saida:/saida

  sonarqube:
    image: sonarqube:community
    container_name: sonarqube
    ports:
      - "9000:9000"
